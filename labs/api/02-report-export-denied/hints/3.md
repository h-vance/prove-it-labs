Reproduce and read the body:

```bash
bash labs/api/_stack/request.sh
```

It returns `"code": "insufficient_scope"` and `"required_scope":
"reports:admin"`, while the detail says her token carries `reports:read`. That
is the entire diagnosis: the default token issued to workspace members cannot
export reports.

Confirm which token does carry it:

```bash
cat labs/api/_stack/credentials.md
```

Swap the token in `labs/api/_stack/request.sh` to prove the diagnosis, then run
`tse check`.

Note carefully what the fix is in real life. You did **not** fix her account by
proving this. You proved which scope she needs so her administrator can grant
it. Handing back an unproven "ask your admin for access" is the weak version of
this answer.
