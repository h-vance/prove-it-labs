Read the exact rejection text:

```bash
docker compose -f labs/docker/_stack/compose.yaml \
               -f labs/docker/_stack/compose.override.yaml \
               logs --tail 20 app
```

It names password authentication for the user. Now prove what each side
believes. What the application is presenting:

```bash
docker compose -f labs/docker/_stack/compose.yaml \
               -f labs/docker/_stack/compose.override.yaml \
               exec app printenv DB_USER DB_PASSWORD
```

And what the database was actually built with:

```bash
docker compose -f labs/docker/_stack/compose.yaml \
               -f labs/docker/_stack/compose.override.yaml \
               exec postgres printenv POSTGRES_USER POSTGRES_PASSWORD
```

Only one side took the rotation. Note which one, because that determines
whether the correct fix is to update the application or to roll the database
credential forward. In this lab the database is the source of truth.
