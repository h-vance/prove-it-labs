name: proveit-docker

services:
  postgres:
    image: postgres:16-alpine
    # Same containment as the app beside it, and for the same reason. The
    # entrypoint drops from root to postgres with gosu, which needs SETUID and
    # is blocked by no-new-privileges, so it starts as postgres instead and
    # never has anything to drop. The named volume stays writable; only the
    # image's own layer is sealed.
    user: postgres
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    environment:
      POSTGRES_DB: support_lab
      POSTGRES_USER: support
      POSTGRES_PASSWORD: demo-password
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./seed.sql:/docker-entrypoint-initdb.d/seed.sql:ro
    tmpfs:
      # The socket directory and scratch space, which are the only paths
      # postgres writes outside its data directory.
      - /var/run/postgresql
      - /tmp
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U support -d support_lab"]
      interval: 2s
      timeout: 3s
      retries: 20
    mem_limit: 256m
    cpus: 0.50
    pids_limit: 100

  app:
    build: .
    environment:
      APP_SECRET: lab-secret
      DB_HOST: postgres
      DB_PORT: "5432"
      DB_NAME: support_lab
      DB_USER: support
      DB_PASSWORD: demo-password
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "127.0.0.1:8100:8080"
    restart: on-failure
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 128m
    cpus: 0.50
    pids_limit: 100
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/health', timeout=2)"]
      interval: 3s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
