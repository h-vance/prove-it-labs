# Single node on purpose. Multi-node teaches scheduling, which is not what the
# support track needs, and it doubles the memory a learner has to spare.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
