---
title: Prove It
description: A hands-on Technical Support Engineering course built on symptom-only tickets, real broken systems, and a grader that shows its evidence.
template: splash
head:
  # Starlight appends the site title, which for this page alone would render as
  # "Prove It | Prove It" in the tab and in search results.
  - tag: title
    content: "Prove It: hands-on Technical Support Engineering"
hero:
  tagline: You get a customer ticket, a genuinely broken system, and no hint about which layer failed. Working out what to look at first is the skill.
  actions:
    # These carry the base path in full, because Starlight puts a hero action's
    # link straight into an href without prefixing it, unlike sidebar entries
    # in astro.config.ts, which it does prefix. Both were written as `/start`
    # and both served a 404 on the published site for as long as it existed.
    - text: Where should I begin?
      link: /prove-it-labs/start
      icon: right-arrow
    - text: How this works
      link: /prove-it-labs/how-it-works
      variant: minimal
    - text: Repository
      link: https://github.com/h-vance/prove-it-labs
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import TrackIndex from "../../components/TrackIndex.astro";
import LabTerminal from "../../components/LabTerminal";
import { loadExercises } from "../../lib/labs";

export const opener = loadExercises().find((exercise) => exercise.id.startsWith("docker/01"));

{/*
  This page used to describe the course. Now it runs it.

  What was here was a stock Starlight splash: a tagline, four cards explaining
  that the tickets are symptom-only and the terminal replays real output, and a
  list of exercises. Every word of it was true and none of it was evidence, on
  a site whose entire argument is that a claim and a demonstration are not the
  same thing. The first thing a reader met was the course talking about itself.

  So the two things it was describing now happen on the page instead, using the
  same components and the same recorded output as the exercise itself. Nothing
  below is written for this page. It is `docker/01`, loaded from `labs/`, which
  means it cannot drift from the exercise and CI re-verifies every byte of that
  output against a freshly provisioned stack on every push.
*/}

## This is the whole idea

A customer writes to you. They tell you what they see, because that is all they
know. Nobody tells you which layer broke.

<div class="ticket" set:html={opener.ticketHtml} />

{/*
  No heading of its own here. LabTerminal renders "Look at the evidence" and a
  lede that says the same thing this paragraph did, so writing one above it put
  two headings and two explanations on the page for one component.
*/}
<LabTerminal exerciseId={opener.id} entries={opener.transcript} client:load />

Try `docker inspect` on the database. It comes back **healthy**, and the
customer still cannot load anything.

Learning to say exactly what that proves, and just as importantly what it does
not, is the entire course.

## Why this is different

<CardGrid>
  <Card title="Symptom-only tickets">
    Every other course names the topic before the exercise, which is the one
    thing a real ticket never does. Here the customer describes what they see,
    and deciding where to look is your job.
  </Card>
  <Card title="The grader shows its evidence">
    A check never just says pass or fail. It prints the assertion, the command
    it ran, and the raw output it evaluated, so being wrong teaches you
    something.
  </Card>
  <Card title="The writing is graded">
    Support is half communication. Customer updates and escalation notes are
    assessed work here, not an afterthought at the end of a lesson.
  </Card>
  <Card title="Built for difficult days">
    Uniform structure, a stated time cost on every exercise before you start,
    and a minimum viable day that still counts as progress.
  </Card>
</CardGrid>

## Start in about two minutes

Open the repository in a GitHub Codespace, wait for the container to build, and
pick up your first ticket:

```bash
tse start docker/01
```

Investigate with ordinary tools. When you think you have fixed it:

```bash
tse check
```

Running locally instead needs Docker and Python 3.9 or newer. There is nothing
to build, and `tse doctor` tells you what is missing, grouped by which track
needs it.

## The exercises

<TrackIndex />
